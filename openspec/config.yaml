# openspec/config.yaml — SDD project configuration
# Auto-generated by sdd-init
schema: spec-driven

context: |
  Project: proyecto-web
  Stack: No code yet — pure Markdown study plans in Plan_de_Estudio/
  Goal: Build a gamified anti-procrastination web app based on the existing study system
  Format: 3 months, ~12 weeks, 84 days, 3 disciplines (English, Spanish, Japanese, Music)
  Conventions: AGENTS.md at root documents study workflow and methodology
  Architecture: TBD — currently flat markdown files, no runtime code

testing:
  strict_tdd: false
  reason: No test runner detected — project has no code yet. TDD requires a testable codebase.
  next: Evaluate testing framework when the web app stack is chosen

rules:
  proposal:
    - Include rollback plan for risky changes
    - Reference existing study plan content when modifying domain logic
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Consider mobile-first approach for gamified UI
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
  apply_config:
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
